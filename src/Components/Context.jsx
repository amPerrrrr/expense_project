import { createContext } from "react";

export const UploadImageAndSelectAvatarContext = createContext(null);

export const AdminCreateNewCategoryContext  = createContext(null);

export const CreateNewExpenseContext  = createContext(null);

export const CreateNewProjectContext  = createContext(null);

export const EditContext = createContext(null);

export const EditProjectContext  = createContext(null);

export const EditAccountantRequestContext  = createContext(null);

export const EditAdminRequestContext  = createContext(null);

export const EditAdminBudgetContext  = createContext(null);

export const AddProjectInEditProjectOneContext  = createContext(null);

export const EditAdminCategoryOneContext  = createContext(null);